apiVersion: v1
kind: Service
metadata:
  name: dpp-web-console
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dpp-web-console
  ports:
    - name: http
      port: 80
      targetPort: http
