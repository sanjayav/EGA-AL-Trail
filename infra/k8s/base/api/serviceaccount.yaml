apiVersion: v1
kind: ServiceAccount
metadata:
  name: dpp-api
  annotations:
    # When using AWS KMS for issuer signing, bind an IRSA role with kms:Sign
    # + kms:GetPublicKey on dpp_kms_key_id. Overlay sets eks.amazonaws.com/role-arn.
    iam.gke.io/gcp-service-account: "<override-in-overlay>"
