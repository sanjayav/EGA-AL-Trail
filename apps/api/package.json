{
  "name": "@dpp/api",
  "version": "0.1.0",
  "private": true,
  "description": "Turborepo wrapper around the FastAPI backend (apps/api). Real source lives in pyproject.toml.",
  "scripts": {
    "dev": "uv run uvicorn dpp_api.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uv run uvicorn dpp_api.main:app --host 0.0.0.0 --port 8000",
    "migrate": "uv run alembic upgrade head",
    "migrate:create": "uv run alembic revision --autogenerate -m",
    "lint": "uv run ruff check dpp_api tests",
    "format": "uv run ruff format dpp_api tests",
    "typecheck": "uv run mypy dpp_api",
    "test": "uv run pytest"
  }
}
